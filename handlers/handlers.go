package handlers

import (
	"net/http"
	"log"
)

func BziHandler(resp http.ResponseWriter, req *http.Request) {
	log.Printf("HUHA")

	switch(req.Method) {
	
	}
}

func OsiHandler(resp http.ResponseWriter, req *http.Request) {
	log.Printf("HUHA")

	switch(req.Method) {
	
	}
}

func PconfigHandler(resp http.ResponseWriter, req *http.Request) {
	log.Printf("HUHA")

	switch(req.Method) {
	
	}
}

func PtemplateHandler(resp http.ResponseWriter, req *http.Request) {
	log.Printf("HUHA")

	switch(req.Method) {
	
	}
}

func MachineHandler(resp http.ResponseWriter, req *http.Request) {
	log.Printf("HUHA")

	switch(req.Method) {
	
	}
}

func CfgHandler(resp http.ResponseWriter, req *http.Request) {
	log.Printf("CFG")

	switch(req.Method) {
	
	}
}

func StateHandler(resp http.ResponseWriter, req *http.Request) {
	log.Printf("CFG")

	/*
	STATE_JSON, err := json.MarshalIndent(curs, "", "  ")
	if err != nil {
		log.Fatal("err: %v, json.Marshal(%v), ", err, curs)
		return
	}
	log.Printf("State below\n%s\n", STATE_JSON)
	*/

	switch(req.Method) {
	
	}
}
