"""Main ``bty`` command-line entry point.

Real subcommands (``bty list``, ``bty inspect``, ``bty flash``, …) land in
later milestones. This is a scaffold so the console-script wiring is
exercised end-to-end.
"""


def main() -> None:
    print("bty: scaffold only — subcommands land in milestone 3+")
