#!ipxe
# bty-web — no machine record for {{ mac }}
#
# Default behaviour: fall back to local boot. The operator can claim
# this MAC by PUT /machines/{{ mac }} on the bty-web HTTP API.

echo no bty assignment for {{ mac }}; falling back to local boot
sleep 5
sanboot --no-describe --drive 0x80 || exit
