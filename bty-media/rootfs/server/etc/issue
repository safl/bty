
======================================================================
  bty __BTY_VERSION__ server appliance

  First boot has not yet completed. The bty-web-init unit will
  rewrite this banner with the browser-UI URL and the default
  ``bty / bty`` credential once the oneshot has run. Reboot if you
  still see this banner on a freshly-imaged disk.
======================================================================

bty __BTY_VERSION__ on \n (\l)
