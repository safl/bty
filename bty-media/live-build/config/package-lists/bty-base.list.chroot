# Minimum packages for a netboot live env.
#
# Phase D-1: just enough to boot to a console. Phase D-2 adds the
# bty runtime + flash dependencies (qemu-utils, nvme-cli, parted,
# zstd, python3, etc.) via a separate package list.
linux-image-amd64
live-boot
live-config
live-tools
